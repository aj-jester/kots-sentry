apiVersion: v1
kind: Service
metadata:
  name: example-nginx
  labels:
    app: example
    component: nginx
spec:
  type: ClusterIP
  ports:
  - port: 8080
    targetPort: 80
    protocol: TCP
    name: http
  selector:
    app: example
    component: nginx
